const express = require('express');
const apiRouter = express.Router();
const labController = require('../controllers/apiControllers/labOverviewController');

// Get Lab for the requested ID
apiRouter.get('/:labId', labController.getLabDescription);

// Get filtered values
apiRouter.get('/filter', labController.filterLabs);

module.exports = apiRouter;
